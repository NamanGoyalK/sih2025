name: sih_internal_app_1
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.5.1

dependencies:
  flutter:
    sdk: flutter
  go_router: ^15.1.2
  flutter_screenutil: ^5.9.3
  provider: ^6.1.5+1
  dio: ^5.9.0
  google_fonts: ^6.3.1
  lottie: ^3.3.2
  syncfusion_flutter_charts: ^31.1.19
  video_player: ^2.10.0
  camera: ^0.10.5+9
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  # Added for localization support
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  # Persist selected language
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # Enable generation of localization delegates from ARB files in lib/l10n
  generate: true
  assets:
    - assets/Sign In.json
    - assets/dummyvid1sih.mp4
    - assets/data/
    - assets/data/current_user.json
